#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void Solve(void) {
    ll n, q, m;
    cin >> n >> m >> q;
    vector<array<ll, 3>> ve(m);
    for (auto &i : ve) {
        cin >> i[0] >> i[1] >> i[2];
    }
    
}
int main(void) {
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

    int cas = 1;
    // cin >> cas;
    while (cas--) Solve();
    return 0;
}